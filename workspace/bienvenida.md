# Bienvenida

Este es un documento de demostración del workspace del agente.

El agente puede listar y leer archivos de texto de esta carpeta con las herramientas
`list_files` y `read_file` (solo lectura).
